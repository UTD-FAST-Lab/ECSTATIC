{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "level.schema.json",
  "title": "Level",
  "description": "An level in a configuration space.",
  "type": "object",
  "required": ["name"],
  "properties": {
    "name": {
      "description": "The name of the level.",
      "type": "string"
    },
    "type": {
      "description": "The type of data the level can accept.",
      "type": "string",
      "enum": ["integer", "enum"]
    },
    "min_value": {
      "description": "If an int, the minimum value it can take on.",
      "type": "integer"
    },
    "max_value": {
      "description": "The largest value allowed for an int.",
      "type": "integer"
    }
  }
}